{% extends "index" %}

{% block content %}

{% set image = entry.musicImage.one() %}

<section class="music-bg" style="background-image: url('{{ image.url }}')">
<div class="music__content content-column">

<h1>{{ entry.musicTitle }}</h1>

</div>
</section>

{% endblock %}
